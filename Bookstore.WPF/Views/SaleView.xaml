<UserControl x:Class="Bookstore.WPF.Views.SaleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bookstore.WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             xmlns:vm="clr-namespace:Bookstore.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SaleViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/XAMLRes/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="BackgroundColor" Color="#F4F7FE"/>
            <DropShadowEffect x:Key="CardShadowEffect" BlurRadius="20" ShadowDepth="0" Opacity="0.08" Color="#4318FF"/>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

            <Style x:Key="PaginationButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="36"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#2B3674"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}" Margin="0, 12,0,12">
        <Grid>
            <Ellipse Width="600" Height="600" Fill="#4318FF" Opacity="0.03" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="-200,-200,0,0"/>
            <Ellipse Width="500" Height="500" Fill="#2B3674" Opacity="0.03" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,-150,-150"/>

            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="0,0,0,12" Padding="24,16" CornerRadius="20" Background="White" Effect="{StaticResource CardShadowEffect}">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <materialDesign:PackIcon Kind="Shopping" Width="32" Height="32" Foreground="#4318FF" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="THÔNG TIN BÁN HÀNG" FontSize="24" FontWeight="Bold" Foreground="#2B3674" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7.5*"/>
                        <ColumnDefinition Width="3.5*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,12,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,12" Padding="16,12" CornerRadius="20" Background="White" Effect="{StaticResource CardShadowEffect}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0" Margin="0,0,12,0">
                                    <TextBox x:Name="txtTimKiem" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Tìm kiếm sách..." FontSize="14" Foreground="#2B3674" Text="{Binding SearchTextSach, UpdateSourceTrigger=PropertyChanged}"/>
                                    <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" Foreground="#A3AED0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                </Grid>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <ComboBox x:Name="cboKieuTimKiem" Width="115" Height="44" materialDesign:HintAssist.Hint="Tìm theo" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="13" Foreground="#2B3674" Margin="0,0,8,0" SelectedItem="{Binding KieuTimKiemSach}">
                                        <ComboBoxItem Content="Tên sách" IsSelected="True"/>
                                        <ComboBoxItem Content="Mã ISBN"/>
                                        <ComboBoxItem Content="Tác giả"/>
                                    </ComboBox>

                                    <ComboBox Width="125" Height="44" materialDesign:HintAssist.Hint="Thể loại sách" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="13" Foreground="#2B3674" Margin="0,0,8,0" ItemsSource="{Binding DanhSachTheLoai}" SelectedItem="{Binding TheLoaiDuocChon, Mode=TwoWay}" DisplayMemberPath="TenTheLoai">
                                    </ComboBox>

                                    <ComboBox x:Name="cboLocGia" Width="120" Height="44" materialDesign:HintAssist.Hint="Khoảng giá" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="13" Foreground="#2B3674" Margin="0,0,8,0" SelectedItem="{Binding LocGiaSach}" ItemsSource="{Binding DanhSachKhoangGia}">
                                    </ComboBox>

                                    <Border Background="#F4F7FE" CornerRadius="10" Padding="10,4" Margin="4,0,4,0" VerticalAlignment="Center" Height="36">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Tổng:" Foreground="#A3AED0" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock x:Name="txtTongBanGhi" Text="{Binding TongSoSach}" FontWeight="Bold" FontSize="14" Foreground="#4318FF" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <Button Command="{Binding XoaBoLocCommand}" CommandParameter="{Binding}" ToolTip="Xóa tất cả bộ lọc" Style="{StaticResource MaterialDesignIconButton}" Width="36" Height="36" IsEnabled="True" Foreground="#EF4444">
                                        <materialDesign:PackIcon Kind="FilterRemove" Width="20" Height="20" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Margin="0,0,0,12" Padding="12" CornerRadius="20" Background="White" Effect="{StaticResource CardShadowEffect}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding DanhSachSachHienThi}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="3" VerticalAlignment="Top" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="6" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#F1F5F9" Effect="{StaticResource MaterialDesignElevationShadow1}">
                                                <Grid Width="173" Height="Auto">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <Grid Grid.Row="0" Cursor="Hand">
                                                        <Grid.InputBindings>
                                                            <MouseBinding MouseAction="LeftClick" Command="{Binding XemChiTietSachCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                                        </Grid.InputBindings>
                                                        <Border CornerRadius="12,12,0,0" ClipToBounds="True">
                                                            <Image Source="{Binding HinhAnh}" Height="140"  Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                        </Border>
                                                    </Grid>

                                                    <Grid Grid.Row="1" Margin="8,8,8,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Cursor="Hand">
                                                            <StackPanel.InputBindings>
                                                                <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.XemChiTietSachCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                                            </StackPanel.InputBindings>
                                                            <TextBlock Text="{Binding TenSach}" TextWrapping="Wrap" FontSize="13" MaxHeight="38" TextTrimming="CharacterEllipsis" FontWeight="Bold" Foreground="#2B3674" Margin="0,0,0,4"/>
                                                                <StackPanel Orientation="Vertical">
                                                                    <TextBlock Text="{Binding DonGiaBan, StringFormat='{}{0:N0} đồng'}" FontWeight="SemiBold" FontSize="14" Foreground="#EF4444" Visibility="{Binding IsNormalPrice, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                    <StackPanel Orientation="Vertical" Visibility="{Binding HasPromotion, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                        <TextBlock Text="{Binding PromotionPrice, StringFormat='{}{0:N0} đồng'}" FontWeight="Bold" FontSize="14" Foreground="#EF4444"/>
                                                                        <TextBlock Text="{Binding DonGiaBan, StringFormat='{}{0:N0} đồng'}" FontSize="12" Foreground="#A3AED0" TextDecorations="Strikethrough"/>
                                                                    </StackPanel>
                                                                    <TextBlock Text=" (Quà tặng)" Foreground="#10B981" FontSize="12" Visibility="{Binding IsGift, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                                </StackPanel>
                                                        </StackPanel>
                                                        <CheckBox Grid.Column="1" VerticalAlignment="Center" Margin="4,0,0,0" IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Command="{Binding DataContext.ChonSachCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>

                        <Border Grid.Row="2" Padding="12,6" CornerRadius="12" Background="White" Effect="{StaticResource CardShadowEffect}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource PaginationButtonStyle}" Command="{Binding FirstPageCommand}" Margin="0,0,4,0">
                                    <materialDesign:PackIcon Kind="ChevronDoubleLeft" Width="16" Height="16" Foreground="#A3AED0"/>
                                </Button>
                                <Button Style="{StaticResource PaginationButtonStyle}" Command="{Binding PrevPageCommand}" Margin="0,0,8,0">
                                    <materialDesign:PackIcon Kind="ChevronLeft" Width="16" Height="16" Foreground="#A3AED0"/>
                                </Button>
                                <ItemsControl ItemsSource="{Binding PageNumbers}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding}" Margin="0,0,4,0" Style="{StaticResource PaginationButtonStyle}" Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Style="{StaticResource PaginationButtonStyle}" Command="{Binding NextPageCommand}" Margin="8,0,0,0">
                                    <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" Foreground="#A3AED0"/>
                                </Button>
                                <Button Style="{StaticResource PaginationButtonStyle}" Command="{Binding LastPageCommand}" Margin="4,0,0,0">
                                    <materialDesign:PackIcon Kind="ChevronDoubleRight" Width="16" Height="16" Foreground="#A3AED0"/>
                                </Button>
                                <TextBlock Text="{Binding CurrentPage, StringFormat='Trang {0}'}" FontSize="12" Foreground="#A3AED0" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="200*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,12" Padding="16" CornerRadius="20" Background="White" Effect="{StaticResource CardShadowEffect}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                    <materialDesign:PackIcon Kind="ShoppingCartOutline" Width="22" Height="22" Foreground="#4318FF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Giỏ Hàng Hiện Tại" FontSize="16" FontWeight="Bold" Foreground="#2B3674" VerticalAlignment="Center"/>
                                    <Border Background="#F4F7FE" CornerRadius="8" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DanhSachGioHang.Count, StringFormat='{}{0} món'}" FontSize="11" FontWeight="Bold" Foreground="#4318FF"/>
                                    </Border>
                                </StackPanel>

                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding DanhSachGioHang}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,8" Padding="10" Background="#F8FAFC" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,4,0">
                                                            <TextBlock Text="{Binding TenSach}" FontSize="13" FontWeight="Bold" Foreground="#2B3674" TextTrimming="CharacterEllipsis"/>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding GiaBan, StringFormat='{}{0:N0} đ'}" FontSize="12" Foreground="#A3AED0" Margin="0,2,6,0" Visibility="{Binding IsNormalPrice, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <StackPanel Orientation="Vertical" Visibility="{Binding IsPromotionApplied, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="{Binding GiaBan, StringFormat='{}{0:N0} đ'}" FontSize="12" Foreground="#10B981" FontWeight="Bold"/>
                                                                    <TextBlock Text="{Binding OriginalGiaBan, StringFormat='{}{0:N0} đ'}" FontSize="11" Foreground="#A3AED0" TextDecorations="Strikethrough"/>
                                                                </StackPanel>
                                                                <TextBlock Text=" (HÀNG TẶNG)" Foreground="#10B981" FontSize="12" Visibility="{Binding IsGift, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                            </StackPanel>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4,0">
                                                            <Button Width="24" Height="24" Padding="0" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#CBD5E1" ToolTip="Giảm số lượng" Command="{Binding DataContext.GiamSoLuongCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                                <materialDesign:PackIcon Kind="Minus" Width="12" Height="12" Foreground="#707EAE"/>
                                                            </Button>
                                                            <TextBlock Text="{Binding SoLuongMua}" FontSize="13" FontWeight="Bold" Foreground="#2B3674" Width="26" TextAlignment="Center" VerticalAlignment="Center"/>
                                                            <Button Width="24" Height="24" Padding="0" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#CBD5E1" ToolTip="Tăng số lượng" Command="{Binding DataContext.TangSoLuongCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                                <materialDesign:PackIcon Kind="Plus" Width="12" Height="12" Foreground="#707EAE"/>
                                                            </Button>
                                                        </StackPanel>

                                                        <Button Grid.Column="2" Width="28" Height="28" Padding="0" Style="{StaticResource MaterialDesignIconForegroundButton}" Foreground="#EF4444" Command="{Binding DataContext.XoaKhoiGioHangCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                            <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16"/>
                                                        </Button>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Padding="16" CornerRadius="20" Background="White" Effect="{StaticResource CardShadowEffect}">
                            <StackPanel>
                                <TextBlock Text="Thông tin hóa đơn" FontSize="15" FontWeight="Bold" Foreground="#2B3674" Margin="0,0,0,10"/>

                                <CheckBox x:Name="chkKhachVangLai" Content="Khách vãng lai" IsChecked="{Binding IsKhachVangLai, Mode=TwoWay}" FontSize="13" FontWeight="Medium" Foreground="#2B3674" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCheckBox}"/>

                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=chkKhachVangLai, Path=IsChecked}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Opacity" Value="0.5"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>

                                    <Grid Margin="0,0,0,8">
                                        <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Số điện thoại khách hàng" Text="{Binding SdtKhachHang, UpdateSourceTrigger=PropertyChanged}" Padding="0,4,25,4" FontSize="13" Foreground="#2B3674"/>
                                        <materialDesign:PackIcon Kind="Phone" Width="18" Height="18" Foreground="#A3AED0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Tên khách hàng" Text="{Binding TenKhachHang, Mode=OneWay}" IsReadOnly="True" Padding="0,4,25,4" FontSize="13" Foreground="#2B3674"/>
                                        <materialDesign:PackIcon Kind="Account" Width="18" Height="18" Foreground="#A3AED0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    </Grid>
                                </StackPanel>

                                <Border BorderBrush="#F4F7FE" BorderThickness="0,1,0,0" Margin="0,0,0,12"/>

                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Tạm tính:" Foreground="#707EAE" FontSize="13" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding TamTinh, StringFormat='{}{0:N0} đ'}" HorizontalAlignment="Right" Foreground="#2B3674" FontSize="14" FontWeight="Bold"/>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Mã ưu đãi:" Foreground="#707EAE" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Width="160" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Nhập mã ưu đãi" Text="{Binding MaUuDai, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Foreground="#2B3674"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="% giảm:" Foreground="#707EAE" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Width="60" TextAlignment="Right" Text="{Binding PhanTramGiam, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignTextBox}" IsReadOnly="True"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <!-- Removed display of original total per request; only show discount amount -->
                                        <TextBlock Text="Giảm:" Foreground="#707EAE" FontSize="12"/>
                                        <TextBlock Text="{Binding GiamTien, StringFormat='{}{0:N0} đ'}" Foreground="#EF4444" FontSize="13" FontWeight="SemiBold"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock Text="Tổng thanh toán:" Foreground="#2B3674" FontSize="14" FontWeight="Bold" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding TongTienThanhToan, StringFormat='{}{0:N0} VNĐ'}" HorizontalAlignment="Right" Foreground="#4318FF" FontSize="18" FontWeight="Black"/>
                                    </StackPanel>
                                </Grid>

                                <Border Background="#2196F3" CornerRadius="6">
                                    <Button Content="Thanh toán" Background="Transparent" BorderThickness="0" Foreground="White" Padding="10,6" 
                                            Command="{Binding MoPopupThanhToanCommand}"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Grid Background="#80000000" Visibility="{Binding IsBookDetailOpen, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Panel.ZIndex="1000">
            <Border Background="#00000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Border Width="550" Padding="24" Background="White" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadowEffect}">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="CHI TIẾT SẢN PHẨM" FontSize="18" FontWeight="Bold" Foreground="#2B3674"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Gray" Command="{Binding CloseDialogCommand}">
                            <materialDesign:PackIcon Kind="Close"/>
                        </Button>
                    </Grid>

                    <Border CornerRadius="8" ClipToBounds="True" HorizontalAlignment="Center" Margin="0,0,0,16" Background="#F4F7FE" Width="160" Height="200">
                        <Image Source="{Binding SachDuocChonXemChiTiet.HinhAnh}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>

                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="Tên sách:" Foreground="#A3AED0"/>
                        <TextBlock Text="{Binding SachDuocChonXemChiTiet.TenSach}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#2B3674"/>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="Mã ISBN:" Foreground="#A3AED0"/>
                        <TextBlock Text="{Binding SachDuocChonXemChiTiet.ISBN}" HorizontalAlignment="Right" Foreground="#2B3674"/>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="Thể loại:" Foreground="#A3AED0"/>
                        <TextBlock Text="{Binding SachDuocChonXemChiTiet.TheLoai}" HorizontalAlignment="Right" Foreground="#2B3674"/>
                    </Grid>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="Giá bán:" Foreground="#A3AED0"/>
                        <TextBlock Text="{Binding SachDuocChonXemChiTiet.DonGiaBan, StringFormat='{}{0:N0} đ'}" HorizontalAlignment="Right" Foreground="#EF4444" FontWeight="Bold"/>
                    </Grid>

                    <Button Content="Thêm vào giỏ hàng" Style="{StaticResource MaterialDesignRaisedButton}" Background="#4318FF" Command="{Binding ThemVaoGioHangTuPopupCommand}" CommandParameter="{Binding SachDuocChonXemChiTiet}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#80000000" Visibility="{Binding IsConfirmingOrder, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Panel.ZIndex="1001">
            <Border Background="#00000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Border Width="600" Padding="28" Background="White" CornerRadius="20" Effect="{StaticResource CardShadowEffect}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Margin="0,0,0,16" BorderBrush="#F4F7FE" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <materialDesign:PackIcon Kind="PlaylistCheck" Width="24" Height="24" Foreground="#4318FF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Xác Nhận Đơn Hàng &amp; Thanh Toán" FontSize="18" FontWeight="Bold" Foreground="#2B3674" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Row="1" Margin="0,0,0,16">
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Khách hàng:" Foreground="#A3AED0" FontSize="13"/>
                            <TextBlock Text="{Binding TenKhachHang}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="#2B3674" FontSize="13"/>
                        </Grid>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Số điện thoại:" Foreground="#A3AED0" FontSize="13"/>
                            <TextBlock Text="{Binding SdtKhachHang}" HorizontalAlignment="Right" Foreground="#2B3674" FontSize="13"/>
                        </Grid>

                        <TextBlock Text="Danh sách sản phẩm mua:" Foreground="#2B3674" FontWeight="Bold" Margin="0,4,0,6" FontSize="13"/>

                        <Border Background="#F4F7FE" CornerRadius="8" Padding="12" MaxHeight="150">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding DanhSachGioHang}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <TextBlock Text="{Binding TenSach}" Foreground="#2B3674" FontWeight="Medium" Width="300" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding SoLuongMua, StringFormat='x{0}'}" Foreground="#A3AED0" HorizontalAlignment="Right" Margin="0,0,100,0"/>
                                                <TextBlock Text="{Binding ThanhTien, StringFormat='{}{0:N0} đ'}" Foreground="#4318FF" FontWeight="Bold" HorizontalAlignment="Right"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#FFF9DB" CornerRadius="12" Padding="16" Margin="0,0,0,24" BorderBrush="#FFE066" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="CHỌN PHƯƠNG THỨC THANH TOÁN:" FontSize="13" FontWeight="Bold" Foreground="#F59E0B" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <RadioButton Content="Tiền mặt" IsChecked="{Binding IsThanhToanTienMat}" Margin="0,0,32,0" FontSize="14" FontWeight="SemiBold" Foreground="#2B3674"/>
                                <RadioButton Content="Chuyển khoản (Ví / Thẻ)" IsChecked="{Binding IsThanhToanChuyenKhoan}" FontSize="14" FontWeight="SemiBold" Foreground="#2B3674"/>
                            </StackPanel>

                            <Border Margin="0,16,0,0" BorderBrush="#FFE066" BorderThickness="0,1,0,0" Padding="0,10,0,0">
                                <Grid>
                                    <TextBlock Text="TỔNG TIỀN PHẢI THU:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Foreground="#2B3674"/>
                                    <TextBlock Text="{Binding TongTienThanhToan, StringFormat='{}{0:N0} VNĐ'}" HorizontalAlignment="Right" FontSize="22" FontWeight="Black" Foreground="#EF4444"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="HỦY BỎ" Style="{StaticResource MaterialDesignOutlinedButton}" Width="120" Height="40" Margin="0,0,12,0" Foreground="#707EAE" BorderBrush="#E2E8F0" Command="{Binding HuyBoGiaoDichCommand}"/>
                        <Button Content="XÁC NHẬN TẠO ĐƠN" Style="{StaticResource MaterialDesignRaisedButton}" Width="180" Height="40" Background="#05CD99" BorderThickness="0" Foreground="White" FontWeight="Bold" Command="{Binding XacNhanTaoDonCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>