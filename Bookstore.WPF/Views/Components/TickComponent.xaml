<UserControl x:Class="Bookstore.WPF.Views.Components.TickComponent"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 xmlns:local="clr-namespace:Bookstore.WPF.Views.Components"
			 x:Name="RootControl"
			 mc:Ignorable="d">

	<Border CornerRadius="6"
			Background="Transparent">

		<CheckBox IsChecked="{Binding IsChecked, ElementName=RootControl}"
				  Command="{Binding Command, ElementName=RootControl}"
				  CommandParameter="{Binding CommandParameter, ElementName=RootControl}"
				  Content="{Binding Text, ElementName=RootControl}"
				  Foreground="{Binding Foreground, ElementName=RootControl}"
				  FontWeight="{Binding FontWeight, ElementName=RootControl}"
				  FontSize="{Binding FontSize, ElementName=RootControl}">

			<CheckBox.Resources>
				<SolidColorBrush x:Key="MaterialDesign.Brush.Primary"
								 Color="#4318FF" />
				<SolidColorBrush x:Key="MaterialDesign.Brush.Secondary"
								 Color="#4318FF" />
				<SolidColorBrush x:Key="PrimaryHueMidBrush"
								 Color="#4318FF" />
				<SolidColorBrush x:Key="SecondaryAccentBrush"
								 Color="#4318FF" />

			</CheckBox.Resources>

		</CheckBox>
	</Border>
</UserControl>